# Coding Guidelines

- Use `StateFlow` for UI state observation in ViewModels.
- Use `AppResult<T>` for all repository and use case return types.
- Ensure all composables use `Modifier` parameter as the first optional parameter.
